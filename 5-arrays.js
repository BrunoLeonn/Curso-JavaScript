// # Arrays: Conjunto de dados(UTILIZA COCHETES "[]")

let familia = [39, 19, 'Bruno', 10, 1];
console.log(familia);
console.log(familia[2], 'tem', familia[1],'anos');

console.log('O lenght desse valor é:', familia.length)

// Lenght significa Tamanho, você consegue visualizar o total de valores que uma string tem